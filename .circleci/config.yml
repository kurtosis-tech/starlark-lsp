version: 2.1
jobs:
  test:
    docker:
      - image: cimg/go:1.18
    steps:
      - checkout
      - run: go test ./...
      - store_test_results:
          path: test-results

workflows:
  version: 2
  build:
    jobs:
      - test
